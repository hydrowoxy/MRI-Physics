# Spinor Visualization

## Run

1. Open a terminal in this folder:

```powershell
cd spinor-visualization
```

2. Start a local server:

```powershell
python -m http.server 8000
```

3. Open this in a browser:

```text
http://localhost:8000
```
